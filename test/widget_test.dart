import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Toom app smoke test', (WidgetTester tester) async {
    // Basic placeholder — full integration tests require a connected device.
    expect(true, isTrue);
  });
}
